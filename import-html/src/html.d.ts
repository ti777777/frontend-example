declare module '*.html' {
  const value: string;
  export default value;// new DOMParser().parseFromString(content).firstElementChild;
}

// declare module '*.json' {
  
//   const content: string;
//   export default new DOMParser().parseFromString(content);
// }
